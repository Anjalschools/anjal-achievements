export type AlumniPrivacySettings = {
  publicProfile?: boolean;
  searchable?: boolean;
  showEmail?: boolean;
  showLinkedIn?: boolean;
  showCompany?: boolean;
  allowMentorshipRequests?: boolean;
};
