/**
 * @deprecated Import from `@/lib/analytics/activity-year-resolver` instead.
 */
export {
  resolveActivityYear,
  formatActivityEditionLabel,
  buildActivityYearLabel,
  extractActivityYearFromAchievement,
  dedupeActivityYears,
  type ActivityYearInput,
  type ResolvedActivityYear,
  type ActivityYearSource,
} from "@/lib/analytics/activity-year-resolver";
