/**
 * Legacy filename kept for discoverability.
 * Letter export uses document-style pages with `report-header.png` (no full-page background).
 * Pagination is layout-based (measured DOM heights), not character counts.
 *
 * Implementation:
 * - `src/lib/letters/paginate-letter-by-layout.ts`
 * - `src/lib/letters/parse-letter-body-blocks.ts`
 * - `src/lib/letters/split-letter-block-by-height.ts`
 * - `src/components/letters/LetterFormalDocument.tsx`
 */

export {};
