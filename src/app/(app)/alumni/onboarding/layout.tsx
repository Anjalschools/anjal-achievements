import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "استكمال بيانات الخريج",
  description: "استكمال المسار الأكاديمي والمهني بعد التخرج.",
};

export default function AlumniOnboardingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
